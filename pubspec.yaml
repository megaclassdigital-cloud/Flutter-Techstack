name: techstack
description: Techstack Hub Flutter App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.1
  flutter_dotenv: ^5.2.1
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1
  google_fonts: ^6.1.0   # tambahkan ini
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - harudusfx.mp3
